using BravoOne.lib;
using BravoOne.UWP.ViewModels.Base;

namespace BravoOne.UWP.ViewModels
{
    public class CreditsViewModel : BaseViewModel
    {
        public CreditsViewModel(GameWrapper wrapper) : base(wrapper)
        {
        }
    }
}