<Page
    x:Class="BravoOne.UWP.Achievements"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BravoOne.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBg}">
        <Grid Background="{StaticResource PanelBg}" BorderBrush="{StaticResource AmberBorder}" BorderThickness="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Padding="16,10,16,10" Background="{StaticResource PanelBg}" BorderBrush="{StaticResource AmberBorder}" BorderThickness="0,0,0,2">
                <TextBlock Text="COMMENDATIONS" Style="{StaticResource Header}" />
            </Grid>

            <Grid Grid.Row="1" Margin="16,10,16,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ListView Grid.Column="0" SelectionMode="None"
                          ItemsSource="{Binding AchievementListing, Mode=TwoWay}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0,3,0,3"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8" Padding="12,10,12,10"
                                  Background="{StaticResource ItemBg}"
                                  BorderBrush="{StaticResource AmberBorder}" BorderThickness="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"
                                       MaxHeight="80" MaxWidth="80" Margin="0,0,12,0"
                                       VerticalAlignment="Center">
                                    <Image.Source>
                                        <BitmapImage UriSource="{Binding ImagePath}" />
                                    </Image.Source>
                                </Image>

                                <TextBlock Grid.Row="0" Grid.Column="1"
                                           Text="{Binding Title}" Style="{StaticResource ListViewHeader}" />

                                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                                    <TextBlock Text="INTEL  " Style="{StaticResource FieldHeader}" />
                                    <TextBlock Text="{Binding Description}" Style="{StaticResource FieldValue}" />
                                </StackPanel>

                                <Border Grid.Column="2" Grid.Row="0" Grid.RowSpan="2"
                                        VerticalAlignment="Center" Padding="8,4,8,4"
                                        BorderThickness="1"
                                        BorderBrush="{StaticResource AmberBorder}">
                                    <Border.Background>
                                        <SolidColorBrush Color="#1A0A00" />
                                    </Border.Background>
                                    <TextBlock FontSize="11" FontWeight="Bold"
                                               FontFamily="Fonts/MainFont.ttf#Tudor Victors"
                                               Text="{Binding ButtonLabel}" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Grid Grid.Column="1" Margin="12,0,0,0" HorizontalAlignment="Right">
                    <Image Source="Assets/SidePanels/Achievements.png" Stretch="Uniform" />
                </Grid>
            </Grid>

            <Grid Grid.Row="2" Padding="16,10,16,10">
                <Button Content="BACK TO MAIN MENU" Click="btnBack_Click" Style="{StaticResource MainButton}" />
            </Grid>
        </Grid>

        <Rectangle IsHitTestVisible="False" Grid.RowSpan="3" Opacity="0.28">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,0.05" SpreadMethod="Repeat">
                    <GradientStop Color="#33000000" Offset="0.0" />
                    <GradientStop Color="#33000000" Offset="0.49" />
                    <GradientStop Color="#00000000" Offset="0.5" />
                    <GradientStop Color="#00000000" Offset="1.0" />
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
    </Grid>
</Page>
